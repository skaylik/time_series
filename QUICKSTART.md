# 🚀 Быстрый старт

Краткое руководство по запуску приложения для анализа временных рядов.

## Установка

### 1. Клонируйте или скачайте проект

```bash
cd TimeSeries
```

### 2. Создайте виртуальное окружение (рекомендуется)

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python -m venv venv
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Сгенерируйте примеры данных

```bash
python generate_sample_data.py
python generate_messy_data.py
```

### 5. Запустите приложение

```bash
streamlit run app.py
```

Приложение откроется в браузере по адресу `http://localhost:8501`

## Быстрый тест функционала

### Тест 1: Чистые данные с анализом

1. Выберите "Использовать пример" → "Температура (дневные данные)"
2. Нажмите "Загрузить пример"
3. Выберите:
   - Дата: `date`
   - Целевая переменная: `temperature`
   - Дополнительные признаки: `humidity`
4. Перейдите к **"Статистический анализ и визуализация"**
5. Просмотрите дескриптивную статистику
6. Выберите признаки для визуализации
7. Изучите все вкладки (Временные ряды, Гистограммы, Box Plots, Корреляции, Q-Q Plots, Scatter Matrix)

### Тест 2: Предобработка грязных данных

1. Загрузите файл `data/messy_temperature.csv`
2. Откройте раздел **"Предобработка данных"**
3. Настройте параметры:
   - ☑️ Удалить дубликаты (первый)
   - ☑️ Обработать пропущенные значения (линейная интерполяция)
   - ☑️ Обработать выбросы (IQR, множитель 1.5, ограничить границами)
4. Нажмите **"Запустить предобработку"**
5. Изучите результаты:
   - Метрики очистки
   - График сравнения до/после
   - Детальные отчёты

### Тест 3: Анализ стационарности

1. Используйте предобработанные данные из Теста 2
2. Перейдите к **"Анализ стационарности"**
3. Выберите целевую переменную
4. Настройте параметры окон (30, 60, 90)
5. Нажмите **"Запустить анализ стационарности"**
6. Изучите результаты:
   - Визуальный анализ тренда
   - Скользящие характеристики
   - Тесты ADF и KPSS
   - Дифференцирование (если необходимо)

### Тест 4: Инженерия признаков

1. Перейдите к **"Инженерия признаков"**
2. Выберите целевую переменную
3. Настройте лаги: 1, 7, 30
4. Выберите скользящие статистики: mean, std (окна 7, 30)
5. Нажмите **"Создать признаки"**
6. Изучите вкладки:
   - Обзор признаков
   - Корреляции лагов
   - Важность признаков
   - Мультиколлинеарность (VIF)
   - Визуализация
7. Скачайте данные с признаками

### Тест 5: ACF/PACF Анализ для ARIMA

1. Перейдите к **"ACF/PACF Анализ для ARIMA"**
2. Выберите переменную для анализа
3. Настройте параметры:
   - Количество лагов: 40
   - Уровень доверия: 95%
   - Применить дифференцирование (если ряд нестационарен)
4. Нажмите **"Запустить ACF/PACF анализ"**
5. Изучите результаты:
   - Графики ACF и PACF с доверительными интервалами
   - Значимые лаги
   - Интерпретация паттернов (AR/MA/ARMA)
   - Рекомендации параметров ARIMA (p, d, q)
6. Используйте рекомендованные параметры для моделирования

### Тест 5.5: Декомпозиция временного ряда

1. Перейдите к **"Декомпозиция временного ряда"**
2. Выберите переменную для декомпозиции
3. Настройте параметры:
   - Период сезонности: 7 (для недельной сезонности)
   - Модель: Аддитивная или Мультипликативная
4. Нажмите **"Запустить декомпозицию"**
5. Изучите результаты во вкладках:
   - **Визуализация компонент**: исходный ряд, тренд, сезонность, остатки
   - **Анализ тренда**: направление, форма, сила (линейный/экспоненциальный)
   - **Анализ сезонности**: период, амплитуда, стабильность паттерна
   - **Анализ остатков**: проверка на белый шум, тесты качества
   - **Сводка**: автоматические выводы и рекомендации
6. Обратите внимание на качество декомпозиции (метрика в верхнем правом углу)

### Тест 6: Полный анализ временного ряда

1. Используйте любые данные
2. Перейдите к **"Параметры анализа временных рядов"**
3. Настройте:
   - Период сезонности: 7 (недельная)
   - Количество лагов: 40
   - Окно скользящего среднего: 7
   - Модель декомпозиции: Аддитивная
4. Нажмите **"Запустить анализ"**
5. Просмотрите результаты:
   - Временной ряд с трендом
   - ACF и PACF
   - Декомпозиция
   - Тесты на стационарность
6. Сгенерируйте HTML-отчёт

## Основные возможности

### ✅ Что вы можете делать:

- 📊 **Загружать данные** в форматах CSV и Parquet
- 🧹 **Очищать данные**: дубликаты, пропуски, выбросы, ресемплирование
- 📈 **Статистический анализ**: дескриптивная статистика, корреляции, тесты нормальности
- 📉 **Визуализация**: гистограммы, box plots, временные ряды, Q-Q plots, scatter matrix
- 🔬 **Анализ стационарности**: тесты ADF и KPSS, дифференцирование, скользящие характеристики
- ⚙️ **Инженерия признаков**: создание лагов, скользящие статистики, анализ важности признаков
- 📊 **ACF/PACF анализ**: определение параметров ARIMA, интерпретация паттернов
- 🧩 **Декомпозиция**: разделение на тренд, сезонность и остатки с детальным анализом
- 🔍 **Анализ временных рядов**: корреляции, тесты стационарности
- 📥 **Экспортировать результаты** в HTML

### 🎯 Рекомендуемый порядок работы:

1. **Загрузка** → Загрузите данные
2. **Предобработка** → Очистите данные (опционально)
3. **Статистика** → Изучите распределение и корреляции
4. **Декомпозиция** → Разделите ряд на тренд, сезонность и остатки
5. **Стационарность** → Проверьте стационарность ряда
6. **ACF/PACF** → Определите параметры ARIMA
7. **Инженерия признаков** → Создайте лаги и скользящие признаки
8. **Анализ TS** → Проведите полный анализ временного ряда
9. **Экспорт** → Сохраните отчёт

## Примеры датасетов

### Чистые данные (для анализа)

- `temperature_daily.csv` - дневные температуры (годовая сезонность)
- `sales_monthly.csv` - месячные продажи (тренд + сезонность)
- `energy_hourly.csv` - почасовое энергопотребление (суточная + недельная сезонность)
- `stock_daily.csv` - дневные цены акций (случайное блуждание)
- `website_traffic_daily.csv` - дневной трафик сайта (недельная сезонность)

### Грязные данные (для предобработки)

- `messy_temperature.csv` - дубликаты, пропуски, выбросы, нарушение монотонности
- `messy_sales.csv` - множественные проблемы в разных колонках
- `messy_sensor.csv` - нерегулярная частота, большие пропуски

## Устранение проблем

### Ошибка импорта модулей
```bash
pip install --upgrade -r requirements.txt
```

### Приложение не запускается
Проверьте версию Python (должна быть ≥ 3.8):
```bash
python --version
```

### Нет данных для визуализации
Убедитесь, что:
- Данные загружены
- Выбраны правильные столбцы
- В данных есть числовые значения

## Дополнительная информация

Для детальной информации см. [README.md](README.md)

## Поддержка

При возникновении проблем:
1. Проверьте README.md
2. Убедитесь, что все зависимости установлены
3. Попробуйте перезапустить приложение

---

**Приятной работы! 📊✨**

